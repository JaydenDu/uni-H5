export enum CardState {
    none = "none",
    applying = "applying",
    apply_passed = "apply_passed",
    apply_reject = "apply_reject",
    succeed = "succeed",
    frozen = "frozen",
    inactive = "inactive",
}
